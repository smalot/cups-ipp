language: php
dist: trusty

php:
  - 5.5
  - 5.6
  - 7.0
  - 7.1

matrix:
  allow_failures:
    - php: 5.5

before_script:
  - sudo apt-get install -yqq cups cups-pdf
  - sudo chpasswd travis:travis
  - composer self-update || true
  - composer update
  - composer --prefer-source --dev update

script: sudo ./vendor/bin/atoum -d tests/ -ncc
